# Setup, deployment and test scripts.
#
# The Lambda function itself needs nothing installed: the Python 3.13 runtime
# already provides boto3, and the handler uses only the standard library
# alongside it.
boto3>=1.35.0
certifi>=2024.7.4
